//Defining our allowedOrigins(line 2), which is equal to am array with an origin inside it(line 3)//
const allowedOrigins = [
    "https://todo-app-muxt.onrender.com"
  ];
  //All of these localhosts(lines 4-6), are the ones we would want to access our rest api//
  module.exports = allowedOrigins;